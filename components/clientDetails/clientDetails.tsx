import { ClientDetails } from '@/interfaces';

export default function ClientDetails({ client }: { client: ClientDetails }) {
	return <div className='flex flex-col gap-4 items-center'>coming soon</div>;
}
